"""Shared prompt fragments used across agents."""

PLAIN_TEXT_RULE = (
    "Respond in plain text only, never Markdown: do not use asterisks, hashes, "
    "backticks, or other formatting markers. Write clear natural language "
    "with simple line breaks."
)
